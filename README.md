# Alternative Use of LLM for Digital Signal Filtering

This repository contains source code for article: `Alternative Use of LLM for Digital Signal Filtering`.

No additional data is required as both traing and testing samples are generated at runtime.

The requirements to run jupyer notebook can be found in `requirements.txt` file.
